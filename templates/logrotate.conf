/var/www/$NEW_USER/log/*.log {
    weekly
    rotate 8
    missingok
    notifempty
    compress
    delaycompress
    dateext
    create 0640 $NEW_USER $NEW_USER
    sharedscripts
	
    postrotate
        systemctl reload php$PHP_VERSION-fpm >/dev/null 2>&1 || true
    endscript
}
